// Team logos keyed by abbreviation
// Add new logos here as: import XXX from './XXX.svg'; and add to the map
import PHX from './PHX.svg';

const teamLogos: Record<string, string> = {
  PHX,
};

export default teamLogos;
